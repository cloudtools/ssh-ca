[DEFAULT]
# Put the remote users you intend to use this key on here
# Can also be specified per environment
principals = ec2-user,ubuntu

[ssh-ca-s3]
region = us-west-1
bucket = your-ssh-config-bucket

[stage]
private_key = ~/.ssh/ssh_ca_stage

[prod]
private_key = ~/.ssh/ssh_ca_prod
